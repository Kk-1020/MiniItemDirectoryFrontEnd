// Used by `ng build` (production).
// Replace this with your deployed backend URL before building for the live demo,
// e.g. 'https://mini-item-directory-api.onrender.com'
export const environment = {
  apiBaseUrl: 'https://REPLACE-WITH-YOUR-BACKEND-URL',
};
